@model IEnumerable<AppBiblioteca.Models.Material>
@{
    ViewData["Title"] = "Libros registrados";
}

<div class="mb-5 pt-5">
    <h2 class=" text-center text-dark">@ViewData["Title"]</h2>
</div>

@{
    var estadoFiltro = ViewBag.EstadoFiltro as string ?? "Todos";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <a asp-action="Create" class="btn btn-success rounded-pill">
        <i class="bi bi-plus-circle"></i> Agregar Nuevo Libro
    </a>

    <div class="d-flex align-items-center gap-2">
        <label for="estadoFiltro" class="mb-0 text-dark fw-bold">
            <i class="bi bi-funnel-fill"></i> Filtrar por Estado:
        </label>
        <select id="estadoFiltro" class="form-select form-select-sm" style="width: auto;" onchange="filtrarPorEstado(this.value)">
            <option value="Todos" selected="@(estadoFiltro == "Todos")">Todos</option>
            <option value="Disponible" selected="@(estadoFiltro == "Disponible")">Disponible</option>
            <option value="Dado de baja" selected="@(estadoFiltro == "Dado de baja")">Dado de baja</option>
        </select>
    </div>
</div>

<div class="mb-3 card-glow">
    <div class="table-responsive">

        <table class="table table-bordered table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Título</th>
                    <th>ISBN</th>
                    <th>Categoría</th>
                    <th>Año de Publicación</th>
                    <th>Idioma</th>
                    <th>Cantidad</th>
                    <th>Disponibles</th>
                    <th>Editorial</th>
                    <th>Autores</th>
                    <th>Estado</th>
                    <th style="text-align:center;">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id_Material</td>
                        <td>@item.Titulo</td>
                        <td>@item.ISBN</td>
                        <td>@item.CategoriaNombre</td>
                        <td>@item.AnioPublicacion</td>
                        <td>@item.Idioma</td>
                        <td>@item.Cantidad</td>
                        <td>@item.Disponibles</td>
                        <td>@item.EditorialNombre</td>
                        <td>@item.Autores</td>
                        <td>@item.Estado</td>
                        <td class="text-center">
                            <div class="d-grid gap-2">
                                <a asp-action="Edit" asp-route-id="@item.Id_Material"
                                   class="btn btn-sm btn-outline-dark rounded-pill"
                                   title="Editar">
                                    <i class="bi bi-pencil-fill"></i> Editar
                                </a>
                                <a asp-action="AgregarEjemplares" asp-route-id="@item.Id_Material"
                                   class="btn btn-sm btn-outline-success rounded-pill"
                                   title="Agregar Ejemplares">
                                    <i class="bi bi-plus-circle"></i> Ejemplares
                                </a>
                                <button type="button"
                                        class="btn btn-sm btn-outline-danger rounded-pill btn-baja"
                                        data-material-id="@item.Id_Material"
                                        data-material-titulo="@item.Titulo"
                                        data-material-disponibles="@item.Disponibles"
                                        title="Dar de Baja Ejemplares">
                                    <i class="bi bi-trash"></i> Dar de Baja
                                </button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
</div>


<!-- Modal para Dar de Baja -->
<div class="modal fade" id="modalBaja" tabindex="-1" aria-labelledby="modalBajaLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="modalBajaLabel">
                    <i class="bi bi-exclamation-triangle"></i> Dar de Baja Ejemplares
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form id="formBaja" asp-action="DarDeBaja" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" id="bajaId" name="id" />
                <input type="hidden" id="bajaCantidadDisponible" />
                <div class="modal-body">
                    <p>Material: <strong id="bajaTitulo"></strong></p>
                    <p class="text-muted small">Cantidad total disponible: <strong id="bajaDisponibleText"></strong></p>

                    <div class="mb-3">
                        <label for="cantidadBaja" class="form-label">Cantidad de ejemplares a dar de baja <span class="text-danger">*</span></label>
                        <input type="number"
                               class="form-control"
                               id="cantidadBaja"
                               name="cantidad"
                               min="1"
                               required
                               placeholder="Ej: 1" />
                        <div class="form-text">Ingrese cuántos ejemplares desea dar de baja</div>
                    </div>

                    <div class="mb-3">
                        <label for="observacion" class="form-label">Motivo de la baja <span class="text-danger">*</span></label>
                        <textarea class="form-control"
                                  id="observacion"
                                  name="observacion"
                                  rows="3"
                                  required
                                  placeholder="Motivo de la baja (deterioro, extravío, obsoleto, etc.)"></textarea>
                    </div>

                    <div class="alert alert-warning" role="alert">
                        <i class="bi bi-info-circle"></i>
                        <strong>Nota:</strong> Esta acción decrementará la cantidad disponible del material y se registrará en el historial de bajas.
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary rounded-pill" data-bs-dismiss="modal">
                        <i class="bi bi-x-circle"></i> Cancelar
                    </button>
                    <button type="submit" class="btn btn-danger rounded-pill">
                        <i class="bi bi-check-circle"></i> Confirmar Baja
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        // Función para filtrar por estado
        function filtrarPorEstado(estado) {
            window.location.href = '@Url.Action("Index", "Materiales")?estadoFiltro=' + encodeURIComponent(estado);
        }

        document.addEventListener("DOMContentLoaded", function() {
          // éxito...
          var ok = '@Html.Raw(TempData["SuccessMessage"])';
          if (ok) {
            Swal.fire({ icon:'success', text: ok });
          }

          // error...
          var err = '@Html.Raw(TempData["ErrorMessage"])';
          if (err) {
            Swal.fire({ icon:'error', title:'Error', text: err });
          }

          // Event listener para los botones de dar de baja
          document.querySelectorAll('.btn-baja').forEach(function(button) {
            button.addEventListener('click', function() {
              var materialId = this.getAttribute('data-material-id');
              var materialTitulo = this.getAttribute('data-material-titulo');
              var cantidadDisponible = parseInt(this.getAttribute('data-material-disponibles'));

              document.getElementById('bajaId').value = materialId;
              document.getElementById('bajaTitulo').textContent = materialTitulo;
              document.getElementById('bajaDisponibleText').textContent = cantidadDisponible;
              document.getElementById('bajaCantidadDisponible').value = cantidadDisponible;
              document.getElementById('cantidadBaja').value = '';
              document.getElementById('cantidadBaja').max = cantidadDisponible;
              document.getElementById('observacion').value = '';

              var modal = new bootstrap.Modal(document.getElementById('modalBaja'));
              modal.show();
            });
          });
        });
    </script>
}